name: Manual Update reserve_faq.json and FAISS

on:
  workflow_dispatch: {}  # 手動実行のみ

permissions:
  contents: write

concurrency:
  group: update-reserve-faq-manual
  cancel-in-progress: true

jobs:
  update-reserve-faq-manual:
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - name: 🧾 リポジトリをチェックアウト
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: 🧹 生成前に必ずクリーン化（衝突防止）
        run: |
          git fetch origin main
          git checkout main
          git reset --hard origin/main
          git clean -fdx

      - name: 🐍 Python をセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 🔧 依存パッケージをインストール
        run: |
          pip install -r requirements.txt
          pip install faiss-cpu python-dotenv

      - name: 🔐 credentials.json を書き出し
        run: echo '${{ secrets.GOOGLE_CREDENTIALS_JSON }}' > credentials.json

      - name: 🧠 reserve_faq.json とインデックスを更新
        run: python update_reserve_faq_and_rebuild.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          FAQ_RANGE: 'reserve_faq!A1:C'
          OUTPUT_PATH: 'data/reserve_faq.json'

      - name: 💾 変更がある場合のみコミット＆プッシュ（pull/rebase しない）
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          # 生成物をステージ
          git add data/reserve_faq.json data/reserve_vector_data.npy data/reserve_index.faiss

          # 変更がなければ終了
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "📘 Manual update: reserve_faq.json & FAISS"
          git push origin HEAD:main

      - name: 🚀 Render に自動デプロイ
        if: success()
        run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
